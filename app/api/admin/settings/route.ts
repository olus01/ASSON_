import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
export async function GET(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const {data:admin}=await supabase.from('admin_profiles').select('user_id').eq('user_id',user.id).eq('is_active',true).maybeSingle();if(!admin)return NextResponse.json({error:'Unauthorized'},{status:401});const {data,error}=await supabase.from('election_settings').select('*').eq('id',1).maybeSingle();if(error)return NextResponse.json({error:'Unable to load settings.'},{status:500});return NextResponse.json({settings:data})}
export async function PATCH(request:Request){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const {data:admin}=await supabase.from('admin_profiles').select('user_id').eq('user_id',user.id).eq('is_active',true).maybeSingle();if(!admin)return NextResponse.json({error:'Unauthorized'},{status:401});const body=await request.json();const allowed=['title','election_title','election_date','opening_time','closing_time','association_name','session_year','committee_name','mode'];const patch=Object.fromEntries(Object.entries(body).filter(([key,value])=>allowed.includes(key)&&value!==undefined));patch.updated_at=new Date().toISOString();const {data,error}=await supabase.from('election_settings').update(patch).eq('id',1).select('*').single();if(error)return NextResponse.json({error:'Could not save settings. Check the available settings fields.'},{status:400});await supabase.from('audit_logs').insert({actor_id:user.id,action:'Election settings updated',entity_type:'election_settings',entity_id:'1',details:{fields:Object.keys(patch)}});return NextResponse.json({settings:data})}
